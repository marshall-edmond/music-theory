import { majorScale } from '../music/MajorFormula';

export const MajorTr = (root:string) => {

    //get the major scale of the root and slice to return triad
    const scale = majorScale(root).filter( note => note.indexOf((note)) === 0 || 2 || 4);

    return scale;
    
}